import { SettingsView } from '@/components/binly/settings-view';

export const metadata = {
  title: 'Settings - Binly Dashboard',
  description: 'Notification settings and history',
};

export default function SettingsPage() {
  return <SettingsView />;
}
